/*
export function getList(page) {
  return global.fetch({
    url: global.contextPath+'-functionmenusvc/api/service/list/'+page.number+'/'+page.size,
    method: 'get'
  });
}

//删除
export function deleteById(id){
  return global.fetch({
    url: global.contextPath+'-functionmenusvc/api/service/'+id,
    method: 'delete'
  });
}

//保存
export function save(data) {
  return global.fetch({
    url: global.contextPath+'-functionmenusvc/api/service/save',
    method: 'put',
    data: data
  });
}

//删除
export function getDataById(id){
  return global.fetch({
    url: global.contextPath+'-functionmenusvc/api/service/'+id,
    method: 'get'
  });
}

export function lookUp(condition) {//视图查询
  return global.fetch({
    url: global.contextPath+'-functionmenusvc/api/object/functionMenu/lookup',
    method: 'PUT',
    data:condition
  });
}

//sql查询
export function querySql(conditon) {
  return global.fetch({
    url: global.contextPath+'-sqlcomputesvc/api/service/querySql',
    method: 'POST',
    data:conditon
  });
}
*/
